import { defineConfig } from 'astro/config';
// import nodejs from '@astrojs/node';

// https://astro.build/config
export default defineConfig({
    site: 'http://t2del.github.io/',
    base: '/blog/',
    // output: 'server',
    // adapter: nodejs(),
});
